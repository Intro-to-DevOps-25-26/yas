# Shared overrides for staging testing.
# Apply together with each chart's base values.yaml.

backend:
  image:
    tag: "0c605cb4"
  serviceMonitor:
    enabled: false
ui:
  image:
    tag: "0c605cb4"
  service:
    type: NodePort
sampledata:
  image:
    tag: "16.3-alpine"
  serviceMonitor:
    enabled: false
