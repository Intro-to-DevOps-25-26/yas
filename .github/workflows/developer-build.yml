name: developer_build

on:
  workflow_dispatch:
    inputs:
      dev_service:
        description: 'Service to deploy with custom branch'
        required: true
        type: choice
        options:
          - cart
          - customer
          - inventory
          - media
          - order
          - product
          - search
          - tax
          - storefront-bff
          - backoffice-bff
          - storefront-ui
          - backoffice-ui
          - swagger-ui
          - sampledata
      dev_branch:
        description: 'Branch name (e.g., dev_tax_service)'
        required: true
        default: main
      image_tag:
        description: 'Optional image tag to deploy (defaults to resolved branch SHA)'
        required: false
        default: ""
      image_digest:
        description: 'Optional immutable image digest from CI/CD (sha256:...)'
        required: false
        default: ""

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      KUBECONFIG_B64: ${{ secrets.KUBECONFIG }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Connect to Tailscale
        uses: tailscale/github-action@6cae46e2d796f265265cfcf628b72a32b4d7cade # v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Set up kubectl
        uses: azure/setup-kubectl@776406bce94f63e41d621b960d78ee25c8b76ede # v4

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "$KUBECONFIG_B64" | base64 -d > $HOME/.kube/config

      - name: Install yq
        uses: mikefarah/yq@1b9b4ac5187171d2e5e3129be0cfa827c7f9d53d # v4

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4

      - name: Log in to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.3.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Validate branch exists
        id: sha
        run: |
          if git ls-remote --exit-code origin ${{ github.event.inputs.dev_branch }} > /dev/null 2>&1; then
            git fetch origin ${{ github.event.inputs.dev_branch }}
            SHA=$(git rev-parse FETCH_HEAD)
            echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          else
            echo "ERROR: Branch '${{ github.event.inputs.dev_branch }}' does not exist on remote origin."
            exit 1
          fi

      - name: Resolve image coordinates
        id: image
        shell: bash
        run: |
          set -euo pipefail
          IMAGE_TAG="${{ github.event.inputs.image_tag }}"
          if [[ -z "$IMAGE_TAG" ]]; then
            case "${{ github.event.inputs.dev_service }}" in
              sampledata) IMAGE_TAG="16.3-alpine" ;;
              *) IMAGE_TAG="${{ steps.sha.outputs.sha }}" ;;
            esac
          fi

          IMAGE_DIGEST="${{ github.event.inputs.image_digest }}"
          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "image_digest=$IMAGE_DIGEST" >> "$GITHUB_OUTPUT"

      - name: Read domain config
        id: config
        run: |
          echo "domain=$(yq -r '.domain' k8s/deploy/cluster-config.yaml)" >> "$GITHUB_OUTPUT"

      - name: Build Helm dependencies
        run: helm dependency build k8s/charts/${{ github.event.inputs.dev_service }}

      - name: Delete existing deployment to avoid SSA conflicts
        run: |
          kubectl delete deployment ${{ github.event.inputs.dev_service }} -n yas --ignore-not-found --wait=true

      - name: Ensure Docker image exists
        run: |
          SERVICE=${{ github.event.inputs.dev_service }}
          TAG=${{ steps.image.outputs.image_tag }}
          DIGEST=${{ steps.image.outputs.image_digest }}
          case "$SERVICE" in
            sampledata) REPO="postgres" ;;
            *) REPO="$DOCKER_USERNAME/yas-$SERVICE" ;;
          esac

          if [ -n "$DIGEST" ]; then
            IMAGE="$REPO@$DIGEST"
          else
            IMAGE="$REPO:$TAG"
          fi

          # Remove local copy if any
          docker image rm -f "$IMAGE" 2>/dev/null || true

          if docker manifest inspect "$IMAGE" > /dev/null 2>&1; then
            echo "Image $IMAGE already exists on Docker Hub"
          else
            case "$SERVICE" in
              storefront-ui|backoffice-ui|swagger-ui)
                echo "Image not found. Pulling from external registry..."
                case "$SERVICE" in
                  storefront-ui) SRC_IMAGE="ghcr.io/nashtech-garage/yas-storefront:latest"  ;;
                  backoffice-ui) SRC_IMAGE="ghcr.io/nashtech-garage/yas-backoffice:latest"  ;;
                  swagger-ui)    SRC_IMAGE="swaggerapi/swagger-ui:v5.11.0"                   ;;
                esac
                docker pull "$SRC_IMAGE"
                docker tag "$SRC_IMAGE" "$IMAGE"
                docker push "$IMAGE"
                ;;
              *)
                echo "ERROR: Image $IMAGE does not exist on Docker Hub."
                echo "Run CI pipeline first, or build manually."
                exit 1
                ;;
            esac
          fi

      - name: Deploy dev service with custom image tag + NodePort
        run: |
          SERVICE=${{ github.event.inputs.dev_service }}
          TAG=${{ steps.image.outputs.image_tag }}
          DIGEST=${{ steps.image.outputs.image_digest }}
          case "$SERVICE" in
            sampledata) REPO="postgres" ;;
            *) REPO="$DOCKER_USERNAME/yas-$SERVICE" ;;
          esac

          # Determine chart type prefix
          case "$SERVICE" in
            storefront-ui|backoffice-ui) PREFIX="ui" ;;
            swagger-ui) PREFIX="" ;;
            sampledata) PREFIX="sampledata" ;;
            *) PREFIX="backend" ;;
          esac

          # Build dynamic --set flags
          SETS="--set-string ${PREFIX:+"$PREFIX."}image.repository=$REPO"
          SETS="$SETS --set-string ${PREFIX:+"$PREFIX."}image.tag=$TAG"
          SETS="$SETS --set-string ${PREFIX:+"$PREFIX."}image.digest=$DIGEST"
          SETS="$SETS --set ${PREFIX:+"$PREFIX."}serviceMonitor.enabled=false"

          # Service type & ingress (Jobs like sampledata don't need service)
          if [ "$SERVICE" != "sampledata" ]; then
            SETS="$SETS --set ${PREFIX:+"$PREFIX."}service.type=NodePort"
            SETS="$SETS --set ${PREFIX:+"$PREFIX."}ingress.enabled=false"
          fi

          helm upgrade --install "$SERVICE" "k8s/charts/$SERVICE" \
            --namespace yas --create-namespace $SETS

      - name: Get NodePort and node IP
        id: svc-info
        run: |
          NODE_PORT=$(kubectl get svc -n yas ${{ github.event.inputs.dev_service }} \
            -o jsonpath='{.spec.ports[0].nodePort}')
          NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[0].address}')
          echo "node_port=$NODE_PORT" >> "$GITHUB_OUTPUT"
          echo "node_ip=$NODE_IP" >> "$GITHUB_OUTPUT"

      - name: Output connection info
        run: |
          echo "## \u2705 Developer Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Service | ${{ github.event.inputs.dev_service }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Branch | ${{ github.event.inputs.dev_branch }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Image tag | ${{ steps.image.outputs.image_tag }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Image digest | ${{ steps.image.outputs.image_digest || 'n/a' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Node IP | ${{ steps.svc-info.outputs.node_ip }} |" >> $GITHUB_STEP_SUMMARY
          echo "| NodePort | ${{ steps.svc-info.outputs.node_port }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Access URL | http://${{ steps.svc-info.outputs.node_ip }}:${{ steps.svc-info.outputs.node_port }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Thêm vào /etc/hosts:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.svc-info.outputs.node_ip }} ${{ github.event.inputs.dev_service }}.yas.local" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
