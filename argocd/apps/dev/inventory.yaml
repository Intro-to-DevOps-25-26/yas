apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: inventory-dev
  namespace: argocd
spec:
  project: yas-dev
  source:
    repoURL: https://github.com/Intro-to-DevOps-25-26/yas.git
    targetRevision: main
    path: k8s/charts/inventory
    helm:
      releaseName: inventory
      values: |
        backend:
          image:
            tag: "latest"
          serviceMonitor:
            enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: yas-dev
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: false
      selfHeal: false
